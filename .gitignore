# ── Build artifacts ──────────────────────────────────────
*.o
*.out
*.exe
*.dSYM/

# ── Compiled binaries (rebuild with make) ───────────────
io_uring_examples/cat_program_io_uring/cat_uring
io_uring_examples/**/build/

# ── Rust / Cargo ─────────────────────────────────────────
io_uring_examples/**/target/

# ── External source trees (checked out locally for study, not our work) ──
wireguard-go/
# linux-source/ IS tracked: it holds the WireGuard module + key kernel files we
# cite as proof in the analysis docs (small, curated — not a full kernel tree).

# ── macOS junk ──────────────────────────────────────────
.DS_Store
._*

# ── Editor/IDE ──────────────────────────────────────────
.vscode/
*.swp
*.swo
*~

# ── OS ──────────────────────────────────────────────────
Thumbs.db
.Trash-*

# ── Generated PDFs (research papers in Documents Read/ are kept) ──
report/*.pdf
*.pdf
!paper/figures/generated/*.pdf
!Documents Read/*.pdf
!resources/instructions_2526.pdf

# ── Archives ────────────────────────────────────────────
*.zip

# ── Benchmark outputs (raw data stays in results/) ─────
*.log
*.csv
!results/**/*.csv
!results/**/*.log
# committed raw data backing the CloudLab findings docs
!data/cloudlab/*.csv
results/
scripts/keys/*_priv
linux/
scripts/keys/multipeer/*_priv
scripts/__pycache__/
notes/full_context_prompt.md

# local plotting venv
.venv-plot/
